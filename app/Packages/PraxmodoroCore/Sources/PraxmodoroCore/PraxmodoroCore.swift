/// PraxmodoroCore: the deterministic timer engine.
/// Pure Foundation — no UI, no network, no persistence imports (spec: timer-engine).
/// Real types arrive with tasks 2.1–2.6; this marker keeps the module buildable.
public enum PraxmodoroCore {
    public static let moduleName = "PraxmodoroCore"
}
