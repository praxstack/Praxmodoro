/// PraxmodoroStore: local-first persistence behind the SessionStoring seam.
/// Real models arrive with tasks 3.1–3.4; this marker keeps the module buildable.
public enum PraxmodoroStore {
    public static let moduleName = "PraxmodoroStore"
}
