# Tasks: add-session-settings

Every task is red-test-first: write the failing test, watch it fail for the right reason, make it pass, run the guard suite. Core tasks verify with `swift test --package-path app/Packages/PraxmodoroCore`; app tasks with `./scripts/focused.sh`. The module-wide no-scheduling and clock-read guards must be green after every task — they are the standing proof that nothing here grew a second clock.

## 1. Engine: custom policies and cadence (Core)

- [x] 1.1 Test: a `TimingPolicy` constructed from user values (e.g. 40/8) derives remaining time identically to a built-in preset shape; reconstruction from persisted transitions matches on simulated relaunch. Implement custom-policy construction as pure data.
- [x] 1.2 Test: long-break cadence (every N blocks, longer length) is policy data — the Nth block-end derives the longer break suggestion; no counter is persisted outside the transition history. Implement cadence derivation.
- [x] 1.3 Test: removing a preset leaves prior session records readable and their derivations unchanged. Implement (likely a no-op proven by test — presets are construction-time data).

## 2. Engine: adjustment transitions (Core)

- [x] 2.1 Test: an `adjustment(+60)` event shifts `remaining(at:)` and `expiryInstant()` by exactly 60s; two `-60` adjustments reconstruct exactly after simulated relaunch. (As-built: `AdjustmentRecord` log alongside transitions, the anomaly-log precedent, because persisted transitions carry only a state name.)
- [x] 2.2 Test: adjustment then sleep through the shifted expiry — `reconciled(at:)` backdates the expiry using the adjusted derivation with zero drift. No new code expected; the test is the point.
- [x] 2.3 Test: adjustments are rejected/ignored for non-running blocks (already-expired, held, closed) — history stays clean. Implement the guard in the transition validator.
- [x] 2.4 Test: a persisted store containing adjustment records replays cleanly under the current schema; schema-parity test still passes untouched.

## 3. Engine: autostart at expiry (Core)

- [x] 3.1 Test: with behaviour "offered default", `reconciled(at:)` past expiry records the break transition at the canonical expiry instant — including when the reconcile happens minutes later (sleep/quit case). Implement autostart in the promotion machinery.
- [x] 3.2 Test: with behaviour "prompt first", expiry records the expiry-hold (place kept) and no break transition until an explicit accept, which records at accept time. Implement.
- [x] 3.3 Test: with behaviour "manual", expiry records nothing — the block-end is presented and the user chooses. (As-built note: today's engine path equals offered-default, so manual is the new conservative option, not the status quo.)
- [x] 3.4 Test: flow policy + autostart "offered default" → zero automatic transitions at any probed instant. The "Flow never auto-ends" guard extended to the autostart path.
- [x] 3.5 Test: behaviour setting is read at expiry-processing time — the same history yields manual/offered/prompt consequences per the behaviour in force at reconciliation (`testBehaviourIsReadAtProcessingTime`, added on validator finding 11).
- [x] 3.6 Historical Core baseline: a later supplied reconciliation instant records enabled auto-return at the canonical break end; disabled (then the factory default) leaves the break open. This pure test did not prove root observation, the process-live fence, sleep/wake wiring, or relaunch absence; task 10.7 owns those current behavior tests after runtime C2.

## 4. Preferences on the defaults seam (App)

- [x] 4.1 Test: `RhythmPreferences` (durations, cadence, autostart behaviour) round-trips through an injected ephemeral `UserDefaults`; factory default is prompt-first with today's durations. Implement the value type on the seam, mirroring the Motion pattern.
- [x] 4.2 **Historical baseline, superseded by owner issue #18 and task 10.5:** `SoundPreferences` (master volume, per-cue toggles, tick loop) round-trips; the then-current factory default was everything OFF. The test must be replaced, not retained as current acceptance.
- [x] 4.3 Test: malformed/missing defaults decode to factory values, never crash, never write back garbage.

## 5. Settings scene (App)

- [x] 5.1 Test: the Settings scene exists with Rhythm and Sound & Notifications panes and opens via ⌘, without disturbing a running session (state-level assertion, not UI automation). Implement the `Settings` scene.
- [x] 5.2 Test: Rhythm pane binds every `RhythmPreferences` field; changes land on the seam. Implement the pane — `SurfacePalette` roles only, zero raw literals.
- [x] 5.3 Test: the flow-exemption notice is present in the pane's rendered content when flow policy exists. Implement.
- [x] 5.4 Test: Sound & Notifications pane binds every `SoundPreferences` field plus notification toggles/texts. Implement the pane.
- [x] 5.5 Test: store schema before/after the whole change is identical (existing schema-parity test re-run and cited in the task commit).

## 6. Block-end flow honours the setting (App)

- [x] 6.1 Test: offered-default — at expiry the break surface appears with decline/end as one ordinary action; the transition instant equals the canonical expiry instant. Wire AppModel to pass behaviour into the engine.
- [x] 6.2 Test: prompt-first — a non-modal offer renders; accept records the break; dismiss keeps the held place; copy passes the tone lint. (As-built: the offer flag rides `SessionSnapshot`, main window only — the capsule and popover stay offer-free by design, they are glanceable not conversational.)
- [x] 6.3 Test: rewind/forward — `+`/`-` during a running block appends adjustments via `CompanionActions`; controls absent when not running. Implement controls and key handling.

## 7. Sound (App)

- [x] 7.1 Commit bundled CC0/original sound assets under `app/Resources/Sounds/` with provenance in the commit message.
- [x] 7.2 **Historical all-off configuration case, not a current factory-default claim:** `SoundPlayer` with an explicitly all-off `SoundPreferences` value produces zero play requests across a full simulated session (seam: a played-cue recorder injected in place of AVFoundation). Implement `SoundPlayer` as a transition listener behind a protocol.
- [x] 7.3 Test: chime enabled — exactly one chime per expiry, none retro-fired after a simulated sleep-through-expiry wake. Implement.
- [x] 7.4 Test: tick loop starts with a running block and stops within one period of hold/close; implemented as an AVAudioPlayer loop toggle, no `Timer` — guard suite proves it. Implement.
- [x] 7.5 Test: resource-load failure leaves the session untouched and logs quietly (os.Logger, debug level). (As-built: device-vanish is untestable without hardware; AVAudioPlayer failures fall into the same silent path.)

## 8. Notifications (App)

- [x] 8.1 Test: notification requests are created from `expiryInstant()` on begin/adjust and cancelled on hold/adjust/setting-change (seam: injected notification-center protocol). Implement scheduling.
- [x] 8.2 Test: shipped default texts pass the copy-tone lint; the lint explicitly exempts the user-text defaults keys. Implement defaults and lint carve-out.
- [x] 8.3 Test: user-edited text is stored and passed to the request verbatim. Implement.
- [x] 8.4 Test: denied permission renders toggles unavailable and schedules nothing; no re-prompt path exists. Implement.

## 9. Registry and gates

- [x] 9.1 **Historical edition-shaped fixture, superseded by owner issue #34 and M2 task 10.0:** the three settings keys were added and proven non-withholdable. Current acceptance uses the one-product configured-key fixture and no Lite/edition API.
- [ ] 9.2 **REOPENED after review repairs.** After section 10 is complete: `./scripts/verify-project.sh` exit 0; `npm run spec:validate` strict; smoke and guard suites green; current evidence recorded before archive. Current non-UI gates and UI compilation are green; a current signed UI run has not passed automation initialization. No auto-push is authorized by this run.
- [ ] 9.3 Independent fresh-context validator reviews the change against the amended spec deltas before archive. Any production/test finding re-enters the uncommitted repair loop: add or retain the red regression, make the minimal fix, rerun `/code-review` and all affected plus full gates, commit the independently testable repair, update evidence, and rerun the validator to COMPLETE. Documentation-only corrections receive the same strict validation and evidence update. No implementation fix may be hidden in the archive/status commit. After archive, replace the new `openspec/specs/session-settings/spec.md` generated `TBD` Purpose with the descriptive paragraph at the top of its delta before rebuilding docs and running final validation. (The 2026-08-13 pass and fixes are historical; the 2026-08-23 pass rejected the current tree and reopened completion.)

## 10. Fresh-validation and owner-decision remediation (2026-08-23)

- [x] 10.1 **Specification freeze.** Reconcile owner issue #18 into proposal, design, capability spec, and this task list: explicitly supersede/reopen the historical factory-off atom 4.2; retain 7.2 only as an explicit all-off configuration test; require five cues, block-start/focus-end/break-end factory-on at 0.7, ticks off, five sample buttons, and legacy saved-choice compatibility. Strict-validate before production work.
- [x] 10.2 **Preset contract — red first.** In `SettingsSceneTests.swift` and `PreferencesTests.swift`, prove each focus/break row can be edited and removed; only unique whole minutes in `1...240` commit; `0`, `241`, fractional, nonnumeric, and duplicate add/edit attempts leave the prior value/input intact beside “Use a unique whole number from 1 to 240.” Prove all configured focus/break Cartesian pairs appear once in `availablePolicies`, a break-only preset uses the classic-focus fallback, and removing a preset after a persisted session leaves that session byte-readable and restores the original derivation. Observe the current edit/bounds/duplicate/history/pair failures; implement through the existing `RhythmPreferences`/`RhythmPane`/`AppModel.availablePolicies` seams only.
- [x] 10.3 **Settings token wiring — red first.** In `SettingsSceneTests`, first use a comment/string-aware balanced-brace helper to require each of `RhythmPane.body` and `SoundNotificationsPane.body` to apply `.foregroundStyle(SurfacePalette.primaryText(increasedContrast: contrast == .increased))` and `.background(SurfacePalette.background(reduceTransparency: reduceTransparency))`, with matching `colorSchemeContrast` and Reduce Transparency environment reads inside the same type. Then rasterize each real pane in a 640×560 frame with fixed preferences, scale 1, sRGB, light color scheme, and standard/increased contrast. Reuse **`add-companion-surfaces` task 10.2's** pixel-difference mask and alpha-blend rules: require at least 20 changed pixels, eight at glyph coverage `>= 0.95`, standard-to-increased pixels matching the two primary-text tokens within `2/255`, the opaque background unchanged within `1/255`, and increased rendered contrast at least 7:1. Separately render Reduce Transparency off/on over a fixed contrasting backdrop and require the on image's pane background to match the opaque token within `1/255` with no sampled backdrop contribution. Observe source, contrast-render, and transparency-render failures before wiring existing palette tokens. Generate tokens only if implementation introduces a new explicit spacing/radius; do not redesign pane layout or add a snapshot dependency.
- [x] 10.4 **Notification denial race — red first.** Use a deferred availability recorder: enable a notification, begin a session so one request is pending, then report `.denied`; assert pending work is cancelled, no replacement is scheduled, and the pane state becomes unavailable. Make the availability callback resynchronize immediately.
- [x] 10.5 **Five-cue preferences and legacy decoding — red first.** In `PreferencesTests.swift`, update factory tests for block-start plus focus-end and break-end chimes on at 0.7 and focus/break ticks off. Add a hand-built legacy JSON blob without `blockStart` proving keyed decode preserves its volume and all five existing fields (`focusTick`, `breakTick`, `focusEndChime`, `breakEndChime`, and `tickLoop`) while defaulting only block-start off; malformed whole blobs alone use factory settings. Observe the current factory and synthesized-decoder failures, then add only the keyed decoder and new preference field.
- [x] 10.6 **Cue transition edges and preview API — red first.** In `SoundDirectorTests.swift`/`ValidatorFindingsTests.swift`, add exactly-once block-start cases for initial begin, explicit end-break, and an already-materialized derived auto-return, with none on relaunch or hold/resume. Add five no-session tests for the exact `AppModel.previewSound(_:)` API asserting `scheduleChime` receives the injected instant/current volume and session/preferences stay byte-equal. Observe the missing cue/API failures, then reuse `SoundCueScheduling.scheduleChime`; add no preview protocol method or second scheduler.
- [x] 10.7 **Live auto-return observation — red first.** Add `observeDerivedPhase(at:)` regressions for both sides of the process-live fence. Construct the model before a break end, observe before it, then advance across focus→break→focus in one jump so the derived phase is running at both observations; require exactly one canonical return transition, `returnPending`, and one block-start cue, with a repeated observation duplicating nothing. Construct/restore a second model after the same break end and prove its first and later observations stay on break with no overlay, cue, or delayed catch-up. Reuse runtime task 3's `liveObservationStartedAt` and bounded `reconciledSession(_:at:)` plumbing; implement `observeDerivedPhase(at:)` and wire the root `TimelineView` date-change callback to pass only `context.date` on every date edge, never only when the derived phase changes. This atom depends on runtime task 3 (#52); keep the no-clock guard green and do not call the observer during restore.
- [x] 10.8 **Five sample controls, resources, and keyboard/VoiceOver evidence — red first.** Give all five pane buttons the identifiers named in design and exact labels `Play Block start sample`, `Play Focus tick sample`, `Play Break tick sample`, `Play Focus end sample`, and `Play Break end sample`. Extend `PraxmodoroUITests/KeyboardLoopUITests.swift` to open Settings, Tab/Shift-Tab to each button, assert both the corresponding label and `hasKeyboardFocus`, and activate it by keyboard. Update resource tests to `SoundCue.allCases`, generate the original block-start WAV through `scripts/generate-sounds.mjs`, and update cue copy/toggles. Observe the missing resource, identifier, label, and signed-UI failures before implementation; run copy-tone and focused checks after green.
- [x] 10.9 **Audio retention and wake cancellation — red first.** Reopen historical task 7.3's unproven device-timebase edge. In `app/Tests/PraxmodoroTests/SoundDirectorTests.swift`, add pure cases for `shouldRetainChime(scheduledAt:now:isPlaying:)`: future+stopped and present+playing are true; past+stopped is false. Add a scheduler regression with future, expired-stopped, and expired-playing entries proving `cancelExpiredChimes(at:)` stops/removes both expired entries and leaves the future one. Add an app-wiring guard for `NSWorkspace.didWakeNotification` calling `AppModel.handleSystemWake()`, which reads the injected clock once, cancels expired chimes, then lets the next root date edge resynchronize presentation. Observe the missing predicate/cancellation/wiring failures. Store pending players with their scheduled instant, inject the existing wall-clock closure through `AudioCueScheduler.init`, and keep ordinary pruning exactly `isPlaying || scheduledAt > now`; add only the wake-cancellation protocol seam, no timer, delegate, or audio framework.
- [x] 10.10 **Evidence repair without invented history.** Preserve checked tasks 1.1–9.1 and their existing commits as immutable baseline evidence; map the grouped historical commits truthfully and record missing contemporaneous red receipts as missing, never reconstructed. Mark factory-off 4.2 superseded by #18 without deleting it. Only new remediation atoms 10.1–10.9 receive current observed red command/failure, green command/result, and one post-review commit SHA. The owner's active-goal pre-approval on 2026-08-23 accepts this documented historical deviation; all implementation, review, test, evidence, and archive gates still apply.
