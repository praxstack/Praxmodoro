# Tasks: add-companion-surfaces

Every new remediation task follows AGENTS.md TDD law: write the named failing test, observe the expected red, implement minimally, and verify green plus the smoke command. Because this run requires review closure before any commit, keep all new production/test work uncommitted until whole-diff `/code-review` is clean; then stage and commit one independently testable remediation atom at a time. Every completed group leaves the repo runnable. Per-atom check: `./scripts/focused.sh`.

## 1. Verification commands

- [x] 1.1 Add `scripts/focused.sh` (app unit suite only) and `scripts/smoke.sh` (build, launch with `-praxmodoro-ephemeral-store`, confirm the window, quit); document both in README next to the existing commands. Red: both scripts absent, `./scripts/focused.sh` exits 127. Green: both exit 0 and the README commands run verbatim. Files: `scripts/focused.sh`, `scripts/smoke.sh`, `README.md`.

## 2. One canonical session state

- [x] 2.1 `SessionSnapshot` value + `AppModel.snapshot(at:)` projecting phase, task line, next action, remaining interval, remaining text, status line and VoiceOver summary; migrate `FocusSurface` and `fieldAccessibilitySummary(at:)` onto it. Red: `SnapshotTests.testSnapshotDerivesRemainingFromTransitions`, `testSnapshotOpenEndedBlockHasNoRemaining` (no `SessionSnapshot` type — compile failure). Green: both pass and every M1 test still passes. Files: `app/Sources/SessionSnapshot.swift`, `app/Sources/AppModel.swift`, `app/Sources/Surfaces/FocusSurface.swift`, `app/Tests/PraxmodoroTests/SnapshotTests.swift`.
- [x] 2.2 Structural single-clock gate. Red: `SnapshotTests.testNoSurfaceCountsTime` fails while any surface formats remaining time locally. Green: the scan finds no `Timer(`, `Timer.publish`, `scheduledTimer`, or stored elapsed counter in `app/Sources`, and every rendered remaining time comes from `snapshot(at:)`. Files: `app/Tests/PraxmodoroTests/SnapshotTests.swift`.
- [x] 2.3 Sleep/relaunch snapshot fidelity. Red: `SnapshotTests.testSnapshotAfterSleepHasNoDrift` before the snapshot routes through `reconciled(at:)`. Green: snapshot remaining equals the canonical-timestamp value across a simulated multi-hour gap. Files: `app/Tests/PraxmodoroTests/SnapshotTests.swift`.

## 3. Surface palette and render-level accessibility

- [x] 3.1 `SurfacePalette` with an explicit Reduce Transparency background branch and an Increase Contrast primary-text branch. Red: `RenderAccessibilityTests.testReduceTransparencyBackgroundIsOpaque` (no `SurfacePalette`). Green: rasterizing the background over a red backdrop with Reduce Transparency on shows no backdrop trace, and differs from the same background with it off. Files: `app/Sources/SurfacePalette.swift`, `app/Tests/PraxmodoroTests/RenderAccessibilityTests.swift`.
- [x] 3.2 Render-level contrast measurement. Red: `RenderAccessibilityTests.testIncreaseContrastRaisesMeasuredRatio`. Green: WCAG ratio computed from rasterized pixels is ≥ 4.5:1 standard and strictly greater under increased contrast. Files: `app/Tests/PraxmodoroTests/RenderAccessibilityTests.swift`.

## 4. Menu-bar popover

- [x] 4.1 `MenuBarPopover` view + `MenuBarExtra` scene rendering the snapshot with phase-appropriate controls. Red: `CompanionSurfaceTests.testPopoverShowsHeldStateAndOffersResume`, `testPopoverWithoutSessionOffersBeginAndNoTime`. Green: both pass. Files: `app/Sources/Surfaces/MenuBarPopover.swift`, `app/Sources/PraxmodoroApp.swift`, `app/Tests/PraxmodoroTests/CompanionSurfaceTests.swift`.
- [x] 4.2 Popover non-scoring and never-paywalled guarantees. Red: `CompanionSurfaceTests.testPopoverCarriesNoScoringOrUpsell`, `testCompanionSurfaceKeysAreNeverPaywalled`. Green: both pass; new feature keys join the never-paywalled set and registry validation covers them. Files: `app/Sources/CapabilityRegistry.swift`, `app/Tests/PraxmodoroTests/CompanionSurfaceTests.swift`.

## 5. Floating focus capsule

- [x] 5.1 `FocusCapsule` view + floating `Window` scene, launch-suppressed, with the capsule command and its keyboard shortcut. Red: `FocusCapsuleTests.testCapsuleShowsTaskTimeAndHold`, `testCapsuleCommandHasKeyboardPath`, `testCapsuleSceneIsFloatingAndLaunchSuppressed`. Green: all pass. Files: `app/Sources/Surfaces/FocusCapsule.swift`, `app/Sources/PraxmodoroApp.swift`, `app/Sources/KeyboardMap.swift`, `app/Tests/PraxmodoroTests/FocusCapsuleTests.swift`.
- [x] 5.2 Three-surface agreement at one instant. Red: `FocusCapsuleTests.testThreeSurfacesAgreeAtOneInstant`. Green: focus, popover, and capsule render identical remaining text, task line, and status line from one snapshot. Files: `app/Tests/PraxmodoroTests/FocusCapsuleTests.swift`.

## 6. Return overlay

- [x] 6.1 `AppModel.returnPending` / `acknowledgeReturn()` raised by `endBreak()`. Red: `ReturnOverlayTests.testEndingBreakRaisesReturnOverlay`, `testAcknowledgementLeavesPhaseUnchanged`. Green: both pass. Files: `app/Sources/AppModel.swift`, `app/Tests/PraxmodoroTests/ReturnOverlayTests.swift`.
- [x] 6.2 `ReturnOverlay` view over the focus surface: exact next action, task fallback, ⏎ dismissal, no evaluation copy. Red: `ReturnOverlayTests.testOverlayShowsRecordedNextAction`, `testOverlayFallsBackToTaskWhenNoNextAction`, `testOverlayCarriesNoBreakVerdict`. Green: all pass. Files: `app/Sources/Surfaces/ReturnOverlay.swift`, `app/Sources/PraxmodoroApp.swift`, `app/Tests/PraxmodoroTests/ReturnOverlayTests.swift`.

## 7. Reduce Motion and VoiceOver on the new surfaces

- [x] 7.1 Motion standdown and VoiceOver labels for popover, capsule, and overlay. Red: `CompanionAccessibilityTests.testCompanionSurfacesStandDownUnderReduceMotion`, `testCompanionSurfacesCarryVoiceOverLabels`, `testAllThreeCompanionSurfacesExist`. Green: the physics model is nil on every companion surface under Reduce Motion and each surface exposes a concise label naming the surface and state. Files: `app/Sources/Surfaces/MenuBarPopover.swift`, `app/Sources/Surfaces/FocusCapsule.swift`, `app/Sources/Surfaces/ReturnOverlay.swift`, `app/Tests/PraxmodoroTests/CompanionAccessibilityTests.swift`.

## 8. Hardening follow-ups from the M1 closeout

- [x] 8.1 User-initiated check-in (⌘K) routed through `openCheckin()`. Red: `HardeningTests.testCheckinHasItsOwnKeyboardPath`. Green: the keyboard map and the command both carry it and the timer-held invariant is unchanged. Files: `app/Sources/KeyboardMap.swift`, `app/Sources/PraxmodoroApp.swift`, `app/Tests/PraxmodoroTests/HardeningTests.swift`.
- [x] 8.2 Two-configuration schema parity against live containers. Red: `HardeningTests.testSchemaParityAcrossStoreConfigurations` (no `containerSchema`). Green: in-memory and on-disk containers expose identical entity names, attribute names, and attribute value types. Files: `app/Packages/PraxmodoroStore/Sources/PraxmodoroStore/LocalStore.swift`, `app/Tests/PraxmodoroTests/HardeningTests.swift`.
- [x] 8.3 (UNBLOCKED 2026-08-13 — the host condition cleared; verify-project.sh ran the signed UI target to green on this machine) Keyboard UI coverage as real key events: begin, ⌘K, check-in `1`–`4`, `R`, ⌘N, capsule toggle. Red: `KeyboardLoopUITests.testWholeLoopByKeyboard` before the paths exist. Green: the UI test walks the loop with no pointer interaction. Files: `app/Tests/PraxmodoroUITests/KeyboardLoopUITests.swift`.
- [x] 8.4 (UNBLOCKED 2026-08-13 — executed in the same green UI run as 8.3) Launch-time first-run assertion. Red: `KeyboardLoopUITests.testFirstRunLaunchPresentsOnlyTheStartPath`. Green: start-path controls present, no running-session control, begin disabled until a task is named, no ambient surface self-opened. Files: `app/Tests/PraxmodoroUITests/KeyboardLoopUITests.swift`.

## 9. Close-out

- [ ] 9.1 **REOPENED 2026-08-23.** A fresh independent validator found four behavior/accessibility defects and one keyboard-evidence gap after the earlier close-out evidence. Complete sections 10–11, then re-run `./scripts/verify-project.sh`, `npm run spec:validate`, `./scripts/smoke.sh`, and a fresh validator before archive. The 2026-08-13 evidence remains historical evidence, not current completion proof.

## 10. Fresh-validation remediation (2026-08-23)

- [x] 10.0 **One-product contract (#34) — red first.** In `CapabilityTests.swift` and `SessionSettingsRegistryTests.swift`, replace edition fixtures with one configured feature-key set; require every `FeatureKey` to resolve available and require a hostile omitted-key fixture to fail `validate()` while lookup self-heals. Add `OneProductContractTests.testProductionHasNoEditionAPI`: recursively scan production `app/Sources/**/*.swift` plus `scripts/generate.sh` and fail on case-insensitive whole-word `edition`, `lite`, `pro`, `enterprise`, `tier`, `license`, `paywall`, `paywalled`, or `upsell`, plus the identifier-level patterns `defaultGrants`, `grants`, `Edition`, `Edition:`, and `[Edition:`; require at least one scanned Swift file and the generator so an empty scan cannot pass. The same test compiles the only supported construction, `CapabilityRegistry(configuredKeys: Set(FeatureKey.allCases))`, making the replacement API a compiler boundary. In `ProvenanceTests.swift`, require About provenance to contain version/SHA/local-first identity and none of that product-tier vocabulary. Observe the current API, source-scan, and copy failures. Remove the dead product-tier API, generated stamp, About label, and residual production comments/copy from `CapabilityRegistry.swift`, `AppModel` construction, `Provenance.swift`, `SettingsSurface.swift`, and `scripts/generate.sh`; preserve the feature-key catalog and validation harness. Align `openspec/config.yaml`, AGENTS, SPEC, README, canonical-delta wording, and `npm run docs:build`; add no replacement tier abstraction.
- [x] 10.1 **Capability consultation — red first.** Add three scene-level computed guards to `PraxmodoroApp`, each returning the direct `model.capabilities.isAvailable` result for exactly one of `.menuBarSurface`, `.focusCapsule`, and `.returnOverlay`. Use those guards as the enclosing `if` condition for the corresponding `MenuBarExtra`, floating `Window`, and root overlay construction. Extend `CompanionSurfaceTests` with a comment/string-aware balanced-brace source helper and three exact assertions: each guard body contains only its matching lookup; the app body contains exactly one matching construction; and that sole construction lies inside the balanced body of `if <matchingGuard>`. Observe all three fail while construction is unconditional. Verify the compiled app still presents all three in the one product and hostile omitted-key fixtures still fail validation while runtime lookup self-heals. Add no parser dependency, second registry, or availability model.
- [x] 10.2 **Increase Contrast wiring — red first.** In `RenderAccessibilityTests`, rasterize the actual `MenuBarPopover` at 320×420, `FocusCapsule` at 320×72, and `ReturnOverlay` at 480×360 with scale 1, sRGB output, a solid `SurfacePalette.background(true)` backdrop, fixed `en_US_POSIX` strings, light color scheme, and `motionStilledOverride: true`; render each once with standard and once with increased `colorSchemeContrast`. Define the text mask as opaque pixels whose maximum RGB delta between renders is at least `3/255`; require at least 20 changed pixels and at least eight whose solved shared glyph coverage is `>= 0.95`. For every masked pixel, solve one glyph-coverage value against the separately rasterized standard/increased `SurfacePalette.primaryText` colors and the sampled opaque background, and require both renders to fit that alpha blend within `2/255` per channel. Require all unmasked background pixels to remain within `1/255`, and compute the full-coverage text/background ratios from the rendered token pixels: increased must exceed standard and be at least 7:1. The popover is the pre-green positive control; capsule and overlay must fail before wiring. Then route capsule and overlay primary text through their `colorSchemeContrast` environment exactly as `MenuBarPopover` does. Keep the existing palette-probe and source-wiring tests as secondary guards.
- [x] 10.3 **One main-window render instant — red first.** In the root `TimelineView` closure, capture exactly one `let snapshot = model.snapshot(at: context.date)`. Route with a pure `model.effectiveSurface(for: snapshot)` overload, pass the immutable value into `FocusSurface`, pass `snapshot.display` into `ReturnOverlay`, and change only the existing presentation callback's input to local `snapshot.phase` so it no longer performs a second snapshot read; keep its existing `syncPresentation(at: context.date)` behavior. Retain `effectiveSurface(at:)` only as a delegating compatibility helper for existing tests. Make the complete focus render, including `RemainingReadout`, consume that value and remove its nested timeline plus `Date()` snapshot property. Add a deterministic unit case that constructs `FocusSurface` with a sentinel snapshot and proves task, next action, phase, prompt, and remaining readout all use it. Add a comment/string-aware balanced-brace source case proving the main timeline closure contains one snapshot call at `context.date`, no other `snapshot(at:)` or raw wall-clock read, and route, focus, overlay, plus the pre-existing callback input use that local; separately require the capsule and popover scene timelines to contain exactly one snapshot call using their own `context.date`. Observe the current mixed main-window/overlay instants and keep module clock guards green. Do not add `observeDerivedPhase(at:)`, date-edge wiring, or its behavior tests here; `add-session-settings` task 10.7 owns all three after runtime C2.
- [x] 10.4 **Return overlay acknowledgement — red first.** Add a `ReturnOverlayTests` case that ends a break, requests a check-in, and proves the model holds once, leaves the focus route and `returnPending` visible, records `checkinPending`, and appends no duplicate hold transition. On `acknowledgeReturn()`, require the overlay and pending flag to clear, route to check-in, and leave the pre-ack held phase unchanged. Reuse `checkinPending`; add no new presentation state.
- [x] 10.5 **Full keyboard evidence — red first.** Give the existing hold, thought input, and break-choice controls stable accessibility identifiers. Extend the signed UI suite with real Space hold/resume, Tab/Shift-Tab traversal to the thought input and a break choice, keyboard-only thought submission/choice, and `XCUIElement.hasKeyboardFocus == true` at each named stop before activation. Preserve existing begin/check-in/break-end/new-session assertions; add no global shortcut or new product action unless the red UI test proves native traversal cannot reach an existing control.

## 11. Remediation close-out

- [ ] 11.1 **REOPENED after review repairs.** Run `swift test --package-path app/Packages/PraxmodoroCore`, `swift test --package-path app/Packages/PraxmodoroStore`, `./scripts/focused.sh`, the full signed UI suite through `./scripts/verify-project.sh`, `./scripts/smoke.sh`, and `npm run spec:validate` from a fresh shell. Current Core, Store, app, format, build, smoke, strict OpenSpec, and UI-build checks are green; the current signed UI run stopped during macOS automation initialization before any test body.
- [x] 11.2 Update `prd.json` and `progress.txt` with the one-product correction, four red→green repairs, keyboard evidence, exact commands, exit codes, and atom SHAs.
- [ ] 11.3 Dispatch a fresh-context validator to map every M2 EARS scenario to evidence. Any production/test finding stops archive and re-enters the red regression → minimal uncommitted repair → `/code-review` plus adversarial review → affected and full fresh gates → isolated staged-tree proof → scoped repair commit → evidence update loop; documentation-only fixes still rerun strict validation. Rerun the validator to COMPLETE, never place implementation in the archive/status commit, and mark 9.1/11.x complete before archive. Archive must sync these deltas into canonical `openspec/specs/`. OpenSpec 1.6 creates a new capability with a placeholder Purpose, so immediately replace `openspec/specs/companion-surfaces/spec.md`'s generated `TBD` Purpose with the descriptive paragraph at the top of its delta and normalize archive-updated canonical Markdown to one trailing newline before rebuilding `docs/site` and running strict validation. Run two stale-term checks: (1) production `app/Sources/**/*.swift`, Core `Sources/**/*.swift`, and `scripts/generate.sh` contain none of the case-insensitive whole-word product vocabulary forbidden by task 10.0; (2) canonical specs, `openspec/config.yaml`, AGENTS, SPEC, README, source/test spec-reference comments, and only their generated counterparts (`docs/site/AGENTS.html`, `README.html`, `SPEC.html`, and `openspec-specs-*.html`) contain none of the deprecated contract phrases `Edition capability registry`, `Edition gating seam`, `Edition-neutral storage`, `UI-free and edition-free`, `edition concerns`, `identical across editions`, `Lite grants`, `Lite edition`, `Pro edition`, `Enterprise edition`, `Lite, Pro, and Enterprise`, `Deterministic versioning and provenance`, `Companion surfaces are never paywalled`, `Settings are part of the one product`, `testSchemaParityAcrossEditions`, or `TBD - created by archiving`. Historical plans, active delta pages that quote `FROM` names, and archived change records are excluded because they retain the migration record.
