# Tasks: stabilize-runtime-contracts

No production code task starts until proposal, delta specs, design, this task list, and the dated plan pass every specification review. Because the user prohibited commits before review closure, red/green work remains uncommitted until the final code-review gate; after that gate, each independently testable atom is committed separately.

## 1. Specification gate

- [x] 1.1 Validate `proposal.md`, all three delta specs, `design.md`, and `tasks.md` with `openspec validate stabilize-runtime-contracts --strict --no-interactive`; expected result: valid.
- [x] 1.2 Run `/codex`, `/grok`, the documented MOA-equivalent council, an independent subagent review, `/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, and `/plan-devex-review` against `docs/plans/2026-08-23-runtime-contract-foundation.md`; record every finding in the review ledger and resolve or explicitly reject it with evidence.
- [x] 1.3 Re-run the spec-creator self-review and strict validation. Implementation is blocked while any critical/major finding or unresolved architecture decision remains.

## 2. Atom #51: payload vocabulary and replay boundary

- [x] 2.1 **Red test — exact files:** add replay and restore-boundary cases to the existing `app/Tests/PraxmodoroTests/LifecycleTests.swift` plus transition-write source guards. Cover known round-trip reconstruction, unknown transition rejection, malformed adjustment rejection, unsupported idle encoding, timestamp-plus-input-index ordering for transitions and adjustments, optional task fallback, explicit unknown review labeling, valid closed replay, closed restore preserving pre-call state, and two sentinel-state atomic restore failures that verify session, identifier, task, action, thoughts, policy, and surface all remain unchanged. The guards SHALL fail unless `AppModel.swift` contains exactly one direct `appendEvent(... kind: .transition ...)` site and SHALL reject bare `"running"`, `"held"`, `"break"`, or `"closed"` payload literals on every transition-write path; durable strings may exist only inside `TransitionPayload`. Run `./scripts/focused.sh` before adding the new source and capture the expected missing `SessionReplay`/codec failures from the existing generated project.
- [x] 2.2 **Minimal implementation — exact files:** add `app/Sources/SessionReplay.swift` as a codec over Core `SessionState`; change `scripts/focused.sh` to run `./scripts/generate.sh` unconditionally so new Swift files cannot be silently omitted by a stale existing Xcode project; add the sole throwing `AppModel.appendTransition(_:at:)` helper, encoding before any store call; update only replay, transition-write, and transition-review-label sites in `app/Sources/AppModel.swift`. Run `./scripts/generate.sh` immediately after creating the file and before the first green build. Do not hand-edit or commit the generated `.xcodeproj`, change SwiftData models, add a repository protocol/store-failure seam, or add package dependencies.
- [x] 2.3 **Green verification:** run `./scripts/focused.sh`; require exactly one direct transition append in `AppModel.swift`, owned by `appendTransition`; require every transition writer to call the helper; run LifecycleTests through the extracted replay path.
- [x] 2.4 **Smoke evidence:** `./scripts/smoke.sh` launches the app with the ephemeral store and exits 0.
- [x] 2.5 **Commit after all review gates close:** `refactor: centralize session replay payloads` with only the #51 files.

## 3. Atom #52: one break-end instant

- [x] 3.1 **Red test — exact files:** add `breakEndInstant` ordinary/cadence/nil-state cases and a custom seven-minute-policy case to `app/Packages/PraxmodoroCore/Tests/PraxmodoroCoreTests/BlockEndBehaviourTests.swift`; change Core call sites to expect `reconciled(autoReturn: Bool, autoReturnAfter: Date?)`. Add boundary cases proving a break end later than the process-live instant returns, one at or before it stays open on repeated reconciliation, and a single reconciliation jump can span focus→break→focus even though the phase is running at both ends. Add one agreement case to `app/Tests/PraxmodoroTests/ValidatorFindingsTests.swift` proving the scheduled sound instant, scheduled notification instant, and materialized auto-return transition are equal at a long-break cadence boundary. Run Core tests and `./scripts/focused.sh`; capture the expected missing-method/API/agreement failures.
- [x] 3.2 **Minimal implementation — exact files:** add `breakEndInstant(cadence:)`, migrate `reconciled(autoReturn:)` to an explicit Boolean gate plus required `autoReturnAfter: Date?`, and use the Core method and boundary in the auto-return branch of `app/Packages/PraxmodoroCore/Sources/PraxmodoroCore/SessionReconstruction.swift`; callers may pass `nil` only with `autoReturn: false`. Capture `AppModel.liveObservationStartedAt` once from its injected clock, make `reconciledSession(_:at:)` pass that non-nil boundary whenever preferences enable auto-return, and replace the two hand-built presentation derivations in `app/Sources/AppModel.swift`. This atom supplies boundary plumbing only; settings task 10.7 owns `observeDerivedPhase(at:)` behavior and root date-edge wiring. Add no timer or app-side break arithmetic.
- [x] 3.3 **Green verification:** run `swift test --package-path app/Packages/PraxmodoroCore` and `./scripts/focused.sh`; require exactly two `breakEndInstant(cadence:)` consumers in `AppModel.swift` (sound and notification), no `breakStart.addingTimeInterval(...)` calculation in either scheduling method, and no independent break-end length arithmetic in Core reconciliation. Prove AppModel's reconciliation plumbing passes the captured process boundary for enabled auto-return and `nil` with the false gate during restore; do not require the later root observer here. The separate `longBreakMinutesDue` suggestion call remains valid.
- [x] 3.4 **Smoke evidence:** `./scripts/smoke.sh` exits 0.
- [x] 3.5 **Commit after all review gates close:** `refactor: derive break end in core` with only the #52 files.

## 4. Atom #53: surface intent boundary

- [x] 4.1 **Red test — exact files:** add behavior and source-scan cases to `app/Tests/PraxmodoroTests/SurfaceTests.swift`. The behavior case SHALL prove `beginNextSession()` routes a reviewed session to initiate without resetting task text or preferences; the source/compiler boundary SHALL require `surface` to be `private(set)` and fail on `model.surface =` under `app/Sources/Surfaces/`. Update the one test-only direct setter to establish state through model behavior. Run `./scripts/focused.sh` and capture the expected failures.
- [x] 4.2 **Minimal implementation — exact files:** make `AppModel.surface` `private(set)`, add `AppModel.beginNextSession()` in `app/Sources/AppModel.swift`, and replace the direct assignment in `app/Sources/Surfaces/ReviewSurface.swift`. Do not introduce an actions protocol or change other surfaces.
- [x] 4.3 **Green verification:** run `./scripts/focused.sh`; run `rg -n 'model\.surface\s*=' app/Sources/Surfaces --glob '*.swift'` and require no matches.
- [x] 4.4 **Smoke evidence:** `./scripts/smoke.sh` exits 0.
- [x] 4.5 **Commit after all review gates close:** `refactor: route surfaces through model intents` with only the #53 files.

## 5. Final review and delivery gate

- [x] 5.1 Run `/code-review` plus an independent fresh-context implementation validator over the complete uncommitted diff. Fix every critical, major, and actionable minor finding; re-run both until no finding remains open.
- [x] 5.2 **REOPENED after review repairs.** Run fresh: `swift test --package-path app/Packages/PraxmodoroCore`, `swift test --package-path app/Packages/PraxmodoroStore`, `./scripts/focused.sh`, `./scripts/verify-project.sh`, `./scripts/smoke.sh`, and `npm run spec:validate`. Fresh pre-archive and post-archive gates passed; the signed post-archive result passed 237/237 with zero failures or skips.
- [x] 5.3 After 5.1 and 5.2 are green, use reviewed hunk staging for shared `AppModel.swift` and test files to create the three clean atom commits named in sections 2–4. Before each commit, require an otherwise empty index; inspect `git diff --cached`; use `git write-tree`, a `mktemp -d` detached worktree at current `HEAD`, and `git read-tree --reset -u <tree>` to materialize the exact staged tree; run that atom's focused checks inside the temporary worktree; remove only that validated temporary worktree; and verify SHA-256 receipts for the unchanged whole reviewed diff and byte-identical residual patch. A green main worktree containing later atoms is not per-commit evidence.
- [x] 5.4 Before archive, mark implementation tasks complete and update `prd.json` and `progress.txt` with actual commands, exit status, test evidence, atom commit SHAs, and issue mapping; do not change acceptance thresholds.
- [x] 5.5 Dispatch an independent fresh-context validator against `SPEC.md`, the active change, committed implementation atoms, and the uncommitted evidence receipts. Any production/test finding stops archive and re-enters the red regression → minimal uncommitted repair → `/code-review` plus adversarial review → affected and full fresh gates → isolated staged-tree proof → scoped repair commit → evidence update loop. Documentation-only fixes still rerun strict validation. Rerun the validator to COMPLETE, and never place an implementation repair in the archive/evidence commit.
- [x] 5.6 Archive `stabilize-runtime-contracts`, then re-run `npm run spec:validate` and the full project gate against the archived tree.
- [x] 5.7 Create the final scoped documentation/status commit for the archive and evidence only. Commit: `f1beb90`.
- [x] 5.8 Reconcile existing GitHub issues #7, #18, and #51–#55 rather than creating duplicates; retain #53's approved `model.surface` boundary, and close only after final committed evidence exists. All seven issues closed 2026-08-26 with implementation commit, archive commit, signed XCTest, smoke, and strict OpenSpec evidence.
