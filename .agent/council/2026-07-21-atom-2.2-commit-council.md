# Atom 2.2 Pre-Commit Shipping Council

## 1. Selected Panel

Torvalds, Musashi, and Feynman in quick mode. All three seats were live. The panel completed the
restatement gate, blind independent Round 1, and anonymized anti-conformity Round 2.

## 2. Chairman

Codex independent Chairman, same-provider/model-tier fallback, non-panel. The Chairman did not
deliberate as any panel member. Verdict: **ACCEPT_CONDITIONALLY**.

## 3. Acceptable Compromises

- Production paid resolution intentionally remains fail-closed until a real verifier atom exists.
- Capability-only publication remains owned by 5.2; Lite-after-Pro data read/export proof remains
  owned by 6.3.
- Private DEBUG grant construction is accepted only because the public symbol graph excludes the
  grant, receipt, lease, and validated testing seams.
- Metadata advancement is a reversible transaction step, not proof that the commit already exists.
- No refactor, feature growth, or polish may enter this milestone transaction.

## 4. Kill Criteria

- If the base is not `0b1316aa7290d114215a1b9ee9876746dfad7036`, invalidated → stop and
  reopen receiving review.
- If any accepted technical or evidence byte changes after review, invalidated → rerun affected
  tests, independent review, and council/receiving checks.
- If any accepted source/test hash or raw xcresult receipt hash does not reproduce, invalidated →
  regenerate evidence and rerun receiving review.
- If the staged index differs from the exact 14-path allowlist below or has unstaged overlap,
  invalidated → reconstruct and re-review the real index.
- If public API exposes validated grant/receipt/lease construction, untrusted claims grant paid
  access, or changed evidence/context preserves an expiry lease, invalidated → reject the atom.
- If focused/full tests, generation/format, unsigned build, release/API-boundary, signed app/UI,
  smoke, gitleaks, strict OpenSpec, JSON, or cached diff gates fail, invalidated → retain Atom 2.2
  as todo and investigate.
- If metadata advances anything beyond Atom 2.2 or disagrees on 4 done, 17 todo, next atom 3.1,
  and the evidence path, invalidated → repair and rerun cached trace review.
- If the commit subject is not exactly `feat: resolve validated product access`, invalidated → do
  not create the milestone commit.
- If postcommit generation/spec/package/status checks fail or the worktree is not clean,
  invalidated → reopen the milestone rather than fixing forward silently.

## 5. Concrete Next Step

Create and independently audit one exact 14-path Git index from this allowlist:

1. `.agent/council/2026-07-21-atom-2.2-commit-council.md`
2. `.agent/evidence/atom-2.2-app-test-receipt.json`
3. `.agent/evidence/atom-2.2-validated-product-access.md`
4. `BLUEPRINT.md`
5. `Packages/PraxodoroCore/Sources/PraxodoroCore/Entitlements/EntitlementSnapshot.swift`
6. `Packages/PraxodoroCore/Sources/PraxodoroCore/Entitlements/ProductRules.swift`
7. `Packages/PraxodoroCore/Tests/PraxodoroCoreTests/EntitlementSnapshotTests.swift`
8. `SPEC.md`
9. `docs/superpowers/plans/2026-07-20-edition-access-foundation.md`
10. `openspec/changes/build-native-praxodoro/design.md`
11. `openspec/changes/build-native-praxodoro/specs/edition-capabilities/spec.md`
12. `openspec/changes/build-native-praxodoro/tasks.md`
13. `prd.json`
14. `progress.txt`

Frozen identities:

- `EntitlementSnapshot.swift`: `91aed09daf3df2c18e55a6932c33b3b1bec659f4f19923e305c44ce90658046b`
- `ProductRules.swift`: `94fcfc51d16dfde30b96126a9835a19f124b787d37e17a0bfd47d340c474f3fb`
- `EntitlementSnapshotTests.swift`: `0487b4ad20b914662f967f90da2e730bae5b2c4dbc17098e9a7b54be8f066cfb`
- Retained bundle: `.build/TestResults/Praxodoro-20260721T120251Z-75165.xcresult`
- Raw summary SHA-256: `94eda0e4ea9cc3876a1ce4d0c1526ca6da3562740d3219181922587728d4fa82`
- Raw tests SHA-256: `2ebc7316e680a21c96f9ad58406bf47f98d11d14d7fcefd3218b723a54a49328`

## 6. Unresolved Questions

No Atom 2.2 technical or normative question remains. The exact staged manifest and cached bytes
still require receiving-code-review. Typed session models, defaults, transition/error matrices,
and complete criterion-to-test traceability remain a hard semantic gate before Atom 3.1.

## 7. Key Agreements

- All three members independently and after anonymized cross-review chose conditional acceptance.
- The implementation is bounded, fail-closed, privately constructible at the verifier seam, and
  coherent with the expiry-only lease tests.
- The fixed normative omission is closed: availability requires implementation, runtime,
  authorization, platform, distribution, and applicable policy.
- Further feature growth before commit has negative value.
- The only remaining uncertainty is transactional: exact bytes, metadata, index, receipt, and gates.

## 8. Key Disagreements

No verdict-level disagreement survived Round 2. Musashi clarified that raw line count is not a
kill criterion; unaudited growth or boundary expansion is. Feynman clarified that strategic delay
is not proof; the cached bytes and receipts determine readiness.

## 9. Decision Options

1. Accept conditionally and execute the frozen transaction — recommended.
2. Revise only if the index review finds concrete drift or a gate regresses.
3. Reject/defer if a specific technical or transactional flaw appears.

## 10. Recommended Next Steps

1. Advance only Atom 2.2 metadata.
2. Stage and audit the exact 14-path allowlist.
3. Reproduce source/test and raw result-receipt hashes plus cached gates.
4. Obtain final receiving-code-review of the real index.
5. Commit with the exact subject, then prove postcommit generation/tests/OpenSpec/clean status.
6. Repair the semantic spec-creator gaps before Atom 3.1.

## 11. Confidence

High (0.97), conditional on exact reproduction of every frozen transaction fact.

## 12. Execution Reliability

Three live seats, zero degraded, zero offline. All seats completed the restatement gate and both
quick rounds. Round 2 used stable anonymized labels plus the verbatim anti-conformity directive;
no seat updated because no peer exposed a specific flaw. The root child-thread limit prevented one
new child creation, so the independent Feynman seat was routed through an available existing agent;
it completed every required round without simulation or degraded output. The Chairman was a
separate non-panel agent. No timeout fallback affected a seat or verdict.

---

## Session Metadata

```yaml
schema_version: 1
mode: quick
panel_size: 3
rounds_run: 2
restatement_gate: true
tools_used: [spawn_agent, followup_task, send_message, wait_agent]
provider_count: 1
fallbacks_triggered: [existing-agent routing for Feynman after child-thread limit]
input_tokens_estimate: ~unknown
output_tokens_estimate: ~unknown
duration_seconds: ~unknown
```
