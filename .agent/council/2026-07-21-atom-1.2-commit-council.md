# Atom 1.2 Pre-Commit Shipping Council

## 1. Selected Panel

Torvalds, Musashi, and Feynman in quick mode. All three seats were live. The panel completed the
restatement gate, blind independent Round 1, and anonymized anti-conformity Round 2.

## 2. Chairman

Codex independent Chairman, same provider/model tier under the single-provider fallback. The
Chairman was not a deliberating panel member.

## 3. Acceptable Compromises

- Test-only cursor anchoring/restoration is acceptable because production code remains identical to
  Atom 1.1 and the UI test retains a direct post-launch heading assertion with no recovery path.
- Single-provider council diversity is acceptable because every seat was live, blind in Round 1,
  stably anonymized in Round 2, and independently reasoned.
- OpenSpec/PRD/progress reconciliation may follow technical review only inside the same guarded
  closure transaction.

## 4. Kill Criteria

- If production `WindowGroup`, `AppPaths`, or app-unit code differs from Atom 1.1, invalidated →
  reopen technical and spec review.
- If the index differs from the exact 13-file allowlist below, invalidated → unstage the difference
  and reopen receiving audit.
- If the named raw JSON artifacts do not reproduce the receipt hashes, invalidated → regenerate
  evidence and rerun review.
- If cached diff, strict OpenSpec, formatting, scope, JSON, or security checks fail, invalidated →
  stop closure and reopen review after remediation.
- If the commit message is not exactly `chore: enforce native project quality gates`, invalidated →
  do not create the milestone commit.
- If postcommit generation produces a diff or scoped status is not clean, invalidated → reopen the
  milestone rather than fix forward.

## 5. Concrete Next Step

Create and audit one exact 13-file Git index from this allowlist:

1. `.agent/council/2026-07-21-atom-1.2-commit-council.md`
2. `.agent/evidence/atom-1.2-app-test-receipt.json`
3. `.agent/evidence/atom-1.2-native-quality.md`
4. `.swift-format`
5. `Praxodoro.xcodeproj/project.pbxproj`
6. `PraxodoroUITests/PraxodoroLaunchUITests.swift`
7. `README.md`
8. `docs/engineering/dependencies.md`
9. `openspec/changes/build-native-praxodoro/tasks.md`
10. `prd.json`
11. `progress.txt`
12. `project.yml`
13. `scripts/verify-project-generation.sh`

Receipt identity:

- Retained bundle: `.build/TestResults/Praxodoro-20260721T112037Z-30323.xcresult`
- Raw summary artifact: `.build/atom-1.2-final-summary.json`
- Raw summary SHA-256: `3de3d19c040b5c680b1e6e8ff43948b20b41c6024d3ce575c1e3826e8a325f8a`
- Raw tests artifact: `.build/atom-1.2-final-tests.json`
- Raw tests SHA-256: `417a7cdc82fe225fb3dd2d180a877d35065dae4cafa53dc56fdacdfb5cf1be55`

## 6. Unresolved Questions

None for Atom 1.2 if every transaction-level kill criterion passes. Later implementation atoms,
release signing, notarization, distribution, and services remain outside this commit.

## 7. Key Agreements

- The frozen nine-path technical candidate is acceptable.
- Three remediated independent reviews and two consecutive 3/3 signed suites are sufficient
  pre-closure evidence.
- Cursor anchoring is acceptable only while production is unchanged and the direct assertion has no
  recovery action.
- Candidate evidence does not prove the commit transaction; the real index and postcommit state do.
- Any closure mismatch requires review reopening.

## 8. Key Disagreements

No panelist retained a substantive technical disagreement. The shared reservation is procedural:
acceptance remains conditional until exact scope, evidence identity, commit identity, regeneration,
and cleanliness are observed.

## 9. Decision Options

1. Accept conditionally and execute the guarded closure — recommended.
2. Reopen review before closure if any frozen-candidate fact cannot be reproduced.
3. Reject the milestone if any kill criterion triggers.

## 10. Recommended Next Steps

1. Reconcile OpenSpec, PRD, and progress without advancing later atoms.
2. Stage and audit exactly the 13-file allowlist.
3. Reproduce receipt hashes and run cached precommit gates.
4. Obtain a final receiving audit of the actual index.
5. Commit with the exact message and prove postcommit regeneration/no-diff plus clean scoped status.

## 11. Confidence

High (0.92). Technical evidence and independent reviews are strong; uncertainty is confined to the
not-yet-executed closure transaction.

## 12. Execution Reliability

Three live seats, zero degraded, zero offline. All seats completed the restatement gate and both
quick rounds. Round 2 used stable labels (`A=Torvalds`, `B=Musashi`, `C=Feynman`) plus the verbatim
anti-conformity directive; all three preserved `ACCEPT_CONDITIONALLY` because no peer exposed a
specific flaw. The Chairman was a separate non-panel call. No timeout, simulation, or retry was
used.

## 13. Session Metadata

```yaml
schema_version: 1
mode: quick
panel_size: 3
panel: [Torvalds, Musashi, Feynman]
chairman: Codex independent chairman
chairman_role: non-panel
rounds_run: 2
restatement_run: true
live_seats: 3
degraded_seats: 0
offline_seats: 0
fallbacks_triggered: single-provider chairman selection only
```
