# Revised Foundation Plan Acceptance Packet

## Decision Requested

Review the council-corrected exact-code plans and return `GO` or
`GO_WITH_REQUIRED_CHANGES`/`NO_GO`. Implementation remains blocked until no major correction is
open.

## Inputs

- `.agent/council/2026-07-20-foundation-plan-council.md`
- `docs/superpowers/plans/2026-07-20-native-foundation.md`
- `docs/superpowers/plans/2026-07-20-native-scaffold.md`
- `docs/superpowers/plans/2026-07-20-edition-access-foundation.md`
- `SPEC.md`, `BLUEPRINT.md`, and `prd.json`
- `openspec/changes/build-native-praxodoro/`

## Corrections Claimed

- Exact official XcodeGen 2.46.0 repo-local bootstrap with tag commit/archive checksum; no
  Homebrew or `jq` dependency.
- Exact toolchain checks, restricted formatter traversal, recursive pre/post generation compare,
  and separate post-commit cleanliness.
- Strict Swift package settings, unsigned app build, executed app-unit/UI test path with retained
  `.xcresult`, isolated smoke state/log/termination, and semantic heading assertion.
- Ungateable exhaustive Lite inventory separated from optional capabilities.
- Exact optional descriptor table across edition/authorization/platform/distribution/data/
  downgrade/implementation dimensions; blocker omitted pending separate add-on spec.
- Public paid claims fail closed because no production verifier exists; internal validated grants
  reject incoherent source/tier/date states.
- Snapshot separates granted/available access and carries status, evidence, issue/expiry, limits,
  missing reasons, policy provenance, and reevaluation.
- Closed managed policy/audit schema; diagnostics/sync consent safe; personal coach cadence absent.
- Paid expiry preserves only committed boundary-scoped session capabilities; safety/privacy and
  other prerequisite changes apply immediately.
- PRD mappings are explicitly supporting evidence, not acceptance claims.

## Falsification Questions

1. Do all shell snippets parse and operate safely, including version parsing, bootstrap retries,
   generation snapshot/cleanup, result bundles, and smoke termination?
2. Does the exact Swift/XcodeGen/SwiftPM code compile under Xcode 26.6 and Swift 6.3.3, or is any
   actor isolation, PackageDescription, Swift syntax, access control, or generated-target claim
   still invalid?
3. Could either test driver report green while package/app/UI tests did not run or the heading was
   not found?
4. Can any public value still self-assert Pro/Enterprise access? Can a future optional capability
   bypass explicit metadata or become available without independent prerequisites/adapter?
5. Can managed configuration carry personal behavior, count a recommendation as privacy consent,
   or preserve unsafe capability access after a safety/privacy change?
6. Did revisions weaken the OpenSpec or root contract, overbuild future services, or leave an
   earlier major finding unresolved?

## Acceptance Rule

`GO` requires no critical/major defect in these first four atoms. Empirical XcodeGen generation,
local test signing, and live XCUITest remain Task 1.1 RED/GREEN evidence and are not assumed from
documentation; the plan must fail and stop if they do not work.

## Outcome

The first review of this packet returned `NO_GO`. After the reconciliations recorded in
`2026-07-20-second-revision-reconciliation.md`, a fresh toolchain, policy/privacy, and trace council
each returned `GO`; see `2026-07-20-final-plan-acceptance.md`.
