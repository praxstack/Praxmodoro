# Atom 2.1 Pre-Commit Shipping Council

## 1. Selected Panel

Torvalds, Musashi, and Feynman in quick mode. All three seats were live. The panel completed the
restatement gate, blind independent Round 1, and anonymized anti-conformity Round 2.

## 2. Chairman

Codex independent Chairman, same-provider/model-tier fallback, non-panel. The Chairman did not
deliberate as any panel member.

## 3. Acceptable Compromises

- Atom 2.1 may close independently while later spec-creator gaps remain open because the accepted
  exact plan fully specifies this catalog.
- Those later contract gaps become a hard gate before Atom 3.1; this verdict does not waive them.
- Status advancement is conditional transaction metadata, not proof that the commit already exists.

## 4. Kill Criteria

- If the base is not `e2a537c`, invalidated → stop and reopen receiving review.
- If any of the three accepted source/test hashes changes, invalidated → stop and reopen technical
  and spec review.
- If the raw summary/tests receipt hashes do not reproduce, invalidated → regenerate the receipt and
  rerun receiving review.
- If the staged index differs from the exact nine-file allowlist below, invalidated → unstage the
  difference and reopen the receiving audit.
- If the catalog is not exactly 29 unconditional Lite features and 12 optional descriptors, or if
  privileged blocking or Atom 2.2 behavior appears, invalidated → reject this atom boundary.
- If focused/full package tests, project generation, strict OpenSpec, cached diff, JSON, formatting,
  shell, or security checks fail, invalidated → stop closure and remediate before re-review.
- If OpenSpec, PRD, and progress advance anything beyond Atom 2.1 or disagree on `3/21` complete and
  next atom `2.2`, invalidated → repair metadata and re-audit the index.
- If the commit message is not exactly `feat: define safe edition access catalog`, invalidated → do
  not create the milestone commit.
- If postcommit generation produces a diff or the worktree is not clean, invalidated → reopen the
  milestone rather than fixing forward silently.

## 5. Concrete Next Step

Create and audit one exact nine-file Git index from this allowlist:

1. `.agent/council/2026-07-21-atom-2.1-commit-council.md`
2. `.agent/evidence/atom-2.1-app-test-receipt.json`
3. `.agent/evidence/atom-2.1-edition-catalog.md`
4. `Packages/PraxodoroCore/Sources/PraxodoroCore/Entitlements/ProductCapability.swift`
5. `Packages/PraxodoroCore/Sources/PraxodoroCore/Entitlements/ProductRules.swift`
6. `Packages/PraxodoroCore/Tests/PraxodoroCoreTests/ProductRulesTests.swift`
7. `openspec/changes/build-native-praxodoro/tasks.md`
8. `prd.json`
9. `progress.txt`

Receipt identity:

- Retained bundle: `.build/TestResults/Praxodoro-20260721T114025Z-84504.xcresult`
- Raw summary SHA-256: `55d2f29dc66b6946fdee35b85c436d50921a052e45fca132d30b26bd650fbe71`
- Raw tests SHA-256: `e22cbe2953c2d0bf56a2fb7d34e06cedb5997115fb1dc28194e667b56c63dee0`

## 6. Unresolved Questions

No Atom 2.1 implementation question remains. Typed session models, configuration defaults,
transition/error matrices, and criterion-to-test traceability remain an explicit pre-Atom 3.1 gate.

## 7. Key Agreements

- Atom 2.1 is a tested edition catalog, not entitlement-resolution architecture.
- The implementation is bounded, reproducible, and free of Atom 2.2 leakage.
- The independent test oracle must remain duplicated from production descriptors; deriving expected
  values from production would weaken regression detection.
- Closure must be one guarded transaction with exact staging, exact metadata, exact commit identity,
  and clean postcommit evidence.

## 8. Key Disagreements

No substantive disagreement survived Round 2. The shared reservation is procedural: acceptance is
conditional until the real index, commit, regeneration, and clean worktree are observed.

## 9. Decision Options

1. Accept conditionally and execute the frozen transaction — recommended.
2. Re-run evidence and review if any frozen fact changed.
3. Reject the milestone if Atom 2.2 leakage, privileged blocking, or a fresh gate failure appears.

## 10. Recommended Next Steps

1. Advance only Atom 2.1 metadata.
2. Stage and audit the exact nine-file allowlist.
3. Reproduce hashes and cached precommit gates.
4. Obtain a final receiving audit of the real index.
5. Commit with the exact message, then prove postcommit generation/no-diff and clean status.

## 11. Confidence

High (0.96), conditional on exact reproduction of every frozen transaction fact.

## 12. Execution Reliability

Three live seats, zero degraded, zero offline. All seats completed the restatement gate and both
quick rounds. Round 2 used stable anonymized labels plus the verbatim anti-conformity directive; no
seat updated because no peer exposed a specific flaw. The Chairman was a separate non-panel call.
No timeout fallback or simulated seat was used.

---

## Session Metadata

```yaml
schema_version: 1
mode: quick
panel_size: 3
rounds_run: 2
tools_used: [spawn_agent, followup_task, wait_agent]
provider_count: 1
fallbacks_triggered: single-provider chairman selection only
input_tokens_estimate: ~unknown
output_tokens_estimate: ~unknown
duration_seconds: ~unknown
```
