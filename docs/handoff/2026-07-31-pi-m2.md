# Handoff: Claude Code → Pi · Praxmodoro M2

> **Superseded 2026-08-04.** The plan changed: M2 was not handed to Pi. It was
> executed in this repository directly, as OpenSpec change
> `add-companion-surfaces` on branch `feat/m2-companion-surfaces`. This document
> is kept as the record of the state M2 started from — its §2 facts were accurate
> at `79914e1`. For current status see `SPEC.md` and `progress.txt`.
>
> Note also that the local folder has since been renamed from
> `~/Developer/Praxodoro` to `~/Developer/Praxomodoro`; the paths below are stale.

Date: 2026-07-31 · Author: Claude Code session (M1 build) · Receiver: Pi

## 1. Objective, scope, repository, authority

**Objective for Pi:** deliver milestone **M2 — companion surfaces** (menu-bar popover, floating focus capsule, return overlay) plus the M1 hardening follow-ups, as a new OpenSpec change, under the same spec-first TDD contract that produced M1.

**Repository:** `/Users/prax/Developer/Praxodoro` (local folder still named Praxodoro; product is **Praxmodoro**). Remote `origin` = `https://github.com/praxstack/Praxmodoro.git` (private). Branch: `main` @ `79914e1`, clean, synced. Remote `legacy` = old `praxstack/PraxoDoro` repo (unrelated Dec-2025 web app + backups) — read-only archive, do not push.

**Binding instructions, in order (per AGENTS.md):** user request → `SPEC.md` + `prd.json` → active change under `openspec/changes/` → current code/tests/git. Work on a feature branch, never directly on `main`. One testable atom per commit with red-then-green evidence recorded in `prd.json` + `progress.txt`. `openspec/config.yaml` carries the artifact rules (EARS scenarios; mandatory error/interruption/sleep-wake/accessibility/privacy/edition-gating cases).

**Not authorized:** force-pushes; pushing to `legacy`; cloud/network/sync/analytics features (M4+, each needs its own spec with explicit opt-in); medical or treatment claims in any copy; paywalling initiation/check-ins/breaks/accessibility (registry validation will fail); deleting research/ or design-mocks/ records; renaming the product again.

## 2. Verified current state (facts, reproduced this session)

- M1 (app scaffold + complete core loop) is **COMPLETE**, confirmed by an independent fresh-context validator on all seven SPEC.md gates at `d56949d`; closeout at `79914e1`.
- `./scripts/verify-project.sh` exits 0: XcodeGen regen + staleness gate, `swift format lint --strict`, PraxmodoroCore 12/12, PraxmodoroStore 7/7, app build, full signed test suite (33 app unit tests + 2 UI tests incl. a real keyboard-driven ⌘↩ begin).
- `npm run spec:validate` strict: 4/4 main specs pass (`openspec/specs/{app-scaffold,timer-engine,focus-loop-ui,session-persistence}` — 27 requirements synced when change `2026-07-31-add-app-scaffold-core-loop` was archived).
- `prd.json`: 28/28 M1 atoms `done` with evidence. `progress.txt`: append-only log through M1 closeout.
- Toolchain quirks you will hit (already solved — do not re-fight them):
  - `.xcodeproj` is generated; edit `app/project.yml`, run `./scripts/generate.sh`.
  - Signing: uniform ad-hoc (`CODE_SIGN_STYLE: Manual`, identity `-`), hardened runtime **off for the two test targets only** — hardened-runtime library validation otherwise rejects the ad-hoc runner loading the ad-hoc bundle ("different Team IDs" is the misleading symptom). App target keeps hardened runtime.
  - UI tests are hermetic via the `-praxmodoro-ephemeral-store` launch argument (in-memory store branch in `PraxmodoroApp.init`).
  - SwiftUI on macOS drops `accessibilityIdentifier` on merged accessibility elements — query the companion field by its VoiceOver label (`label BEGINSWITH 'Companion:'`).
  - `generate.sh` stamps `app/Sources/Generated/BuildProvenance.swift` (gitignored).

## 3. Durable artifacts

- Contract: [SPEC.md](../../SPEC.md) (M1 gates + M2–M5 milestone list + global invariants) · [prd.json](../../prd.json) · [AGENTS.md](../../AGENTS.md)
- Specs: `openspec/specs/*/spec.md` (main, authoritative) · archived M1 change: `openspec/changes/archive/2026-07-31-add-app-scaffold-core-loop/`
- Evidence trail: `progress.txt` (append-only) · git history `9a91032..79914e1` (conventional commits, one atom each)
- Design truth: `design-mocks/living-companion/` (approved direction B; physics contract `companion-physics.js` — the Swift port is golden-tested against it) · comparative records `design-mocks/hallmark/`, `design-mocks/focus-observatory/` · decision record `praxmodoro-direction-gate.md` + `.dcanvas/`
- Research (product claims must trace here): `research/pomodoro-landscape-20260720/outputs/`

## 4. One concrete next action + acceptance criteria

**Next action:** create OpenSpec change `add-companion-surfaces` (`npx openspec new change "add-companion-surfaces"`), author proposal → specs → design → tasks per `openspec/config.yaml`, covering:

1. **Menu-bar popover** — canonical session state, start/hold/check-in entry, no scoring; mock reference: surfaces screen of the Living Companion mock set.
2. **Floating focus capsule** — always-on-top minimal timer + task line; same engine truth, presentation only.
3. **Return overlay** — post-break re-entry card surfacing the exact next action.
4. **M1 hardening follow-ups** (recorded in `progress.txt` closeout entry): render-level Reduce-Transparency/Increase-Contrast verification; real two-configuration schema-parity test; keyboard UI coverage beyond begin (check-in 1–4, `R`, `⌘N`); launch-time first-run assertion.

**Acceptance (mirror of M1's gate pattern):** every EARS scenario in the new change covered by a passing automated test; `./scripts/verify-project.sh` exits 0; `npm run spec:validate` strict passes; all three surfaces share one canonical session state (no live-tick duplication); complete accessibility alternates ship in the same change; `prd.json`/`progress.txt` carry per-atom red→green evidence; an independent fresh-context validator confirms before the change is archived.

## 5. Facts vs recommendations

Everything in §2 is fact (reproduced this session). **Recommendations** (yours to overrule): keep `FieldPhysics`/`FieldModel` as the single motion source for capsule/popover renderings rather than new animation code; treat `NSStatusItem` + `MenuBarExtra` trade-offs as a design.md decision with alternatives; benchmark the Canvas field at 120 Hz before considering Metal (open research handoff item); the mock's layer-separation is softer than the native render — a visual-polish atom inside M2 is worth scoping.

## 6. On receipt

Re-verify before trusting: run `./scripts/verify-project.sh` and `npm run spec:validate` fresh; skim `git log --oneline -20`. Do not trust this document's claims you cannot reproduce.

